# async.js — Asynchronous JavaScript Demo

`async.js` is a simple JavaScript file that demonstrates how asynchronous operations work in JavaScript using **callbacks**, **promises**, and **async/await**.  
This project is useful for beginners learning JavaScript asynchronous programming, API calls, and event loops.

---

## 🚀 Features

- Demonstrates how JavaScript handles non-blocking operations  
- Shows usage of:
  - `setTimeout()`
  - Callbacks
  - Promises (`resolve`, `reject`)
  - `async` / `await`
  - Error handling using `try...catch`
- Clean, easy-to-understand examples

---



